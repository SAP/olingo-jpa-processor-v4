language: java
jdk:
    - oraclejdk8
cache:
  directories:
    - $HOME/.m2/repository
script:
    - cd study-design-backend && mvn --settings ../travis/settings.xml clean verify
branches:
  only:
  - master
  - develop	