logging:
  level:
    org:
      springframework: DEBUG
odata:
  jpa:
    punit_name: ${punit}
    root_packages: ${package}
server:
  port: 9010
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  flyway:
    enabled: true
    schemas:
    - test
  servlet:
    multipart:
      enabled: false
